// package Recursion;

public class StringPalindrome {
    public String check(String str) {
        String st = "";
        // if(str.length()==0){
        //     return "";
        // }
        // if(str.charAt(str.length()-1)==str.charAt(0)){

        // }
        return st;
        
    }
}
